============
Installation
============

At the command line::

    $ easy_install tastypie_signals

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tastypie_signals
    $ pip install tastypie_signals